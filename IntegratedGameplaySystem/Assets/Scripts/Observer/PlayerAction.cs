namespace DesignPatterns
{
    public enum PlayerAction
    {
        None = 0,
        PrimaryFire = 1,
        SecondaryFire = 2,
        Reload = 3,
        Inspect = 4 
            // Etc, etc ...
    }
}